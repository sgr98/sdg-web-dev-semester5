const mocha = require('mocha')
const assert = require('assert')

// Describe Tests
describe('some demo test', () => {

    // Create tests
    it('add two numbers togethers1', () => {
        assert(2 + 4 === 6)
    })

    // it('add two numbers togethers2', () => {
    //     assert(2 + 4 === 5)
    // })

})